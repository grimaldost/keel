---
description: Apply the keel method to the current project (bind slots, run the phases).
---

Invoke the `apply-method` skill to set up or run the keel method here: read `docs/doctrine.md`,
then bind every portability slot (`src/keel/templates/method-bindings.md`) and walk the 8 phases.
$ARGUMENTS
