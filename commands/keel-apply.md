---
description: Apply the keel method to the current project (bind slots, run the phases).
---

Invoke the `apply-method` skill to set up or run the keel method here: read the doctrine at
`${CLAUDE_PLUGIN_ROOT}/docs/doctrine.md`, run `keel init` to copy the template kit into this
project, then bind every portability slot in the COPIED `method-bindings.md` (in this project, not
the packaged template) and walk the 8 phases.
$ARGUMENTS
