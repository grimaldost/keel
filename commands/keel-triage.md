---
description: Triage a series' reflections and promote recurring traps to durable checks.
---

Run the reflection-triage procedure from the copy `keel init` placed in this project
(`reflection-triage.md`; re-emit the kit with `uvx --from ${CLAUDE_PLUGIN_ROOT} keel init` if it
is absent) over the series' feedback: sweep the sink's open rows, cluster by cause, promote each
recurring trap to exactly one of a review-checklist item, a guardrail/gate, or a spec-template
change, and land it per the template's landing rule — apply directly when the target is editable
in-context; otherwise hand off to the method's feedback intake.
CHANGELOG entries belong to the method repo at build time, not to this project.
$ARGUMENTS
